#服务端口
server:
  port: 8080
# 应用名称
spring:
  application:
    name: StudentSystem
  #数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/student_system?userSSL=false&charset=utf-8&serverTimezone=GMT
    username: root
    password: 13597803422dw

#下面这些内容是为了让MyBatis映射
#指定Mybatis的Mapper文件
mybatis:
  #映射文件
  mapper-locations: classpath:mapper/*xml
  #指定Mybatis的实体目录
  type-aliases-package: com.chunfeng.entity

